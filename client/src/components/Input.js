import FileBase from 'react-file-base64';

// export const Input = ({ allState, setAllState }) => {
//     return (
//         <div style={{width: '0px !important'}} className='w-0 h-0'>
//             <FileBase  type='file' multiple={false} onDone={({ base64 }) => setAllState({ ...allState, img: base64 })} />
//         </div>
//     )
// }

export default FileBase